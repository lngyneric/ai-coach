<!-- Generated by scripts/generate_ai_collab_docs.py. Edit metadata in that script. -->

# Backend Service: gen_mdf

This file adds local guidance for `src/api/flaskr/service/gen_mdf`. Read the
repository root and `src/api/AGENTS.md` first, then use this file for
module-specific ownership and risk points.

## Scope

- This service owns the backend proxy that converts text through the external
  MDF service while hiding provider details from the browser.

- Primary checked-in entry files today:
  `src/api/flaskr/service/gen_mdf/route.py`,
  `src/api/flaskr/service/gen_mdf/funcs.py`

- Keep behavior in this module unless the concern is clearly cross-service and
  shared by multiple backend domains.

- Preferred pytest location: `src/api/tests/service/gen_mdf/` does not exist
  yet; create it when this service gains or changes behavior.

## Do

- Start from the local entry files and preserve how this service already
  structures helpers, models, routes, and DTO-like helpers.

- Keep the frontend talking to the backend proxy instead of calling the
  external MDF API directly.

- Preserve validation of text length, language, and request timeout before
  proxying to the external service.

- Return errors in a form that the frontend can localize and present without
  leaking the upstream endpoint configuration.

## Avoid

- Do not surface the external MDF URL or secret config values to frontend code
  or browser-visible payloads.

- Do not weaken proxy validation or timeout handling when adding new request
  options.

- Do not add frontend-specific formatting rules here when the service should
  stay a thin backend proxy boundary.

- Avoid scattering service-specific rules upward into root docs when the
  behavior only matters inside this module.

## Commands

- `rg -n "gen_mdf" src/api/flaskr/service src/api/tests` to locate all backend
  call sites and tests before editing.

- `cd src/api && pytest -q` until `src/api/tests/service/gen_mdf/` exists;
  create that directory first when this service gains behavior that needs
  focused regression coverage.

- `cd src/api && pytest -q` is usually enough unless work here also changes
  shared models in another service.

- `cd src/api && pytest -q` when a change touches shared DTOs, auth, streaming
  contracts, or other services in addition to this module.

## Tests

- Add or update focused tests around `src/api/tests/service/gen_mdf/` for the
  happy path and the highest-risk validation, permission, or failure case.

- Expand coverage when payload shapes, error codes, or shared helper contracts
  used outside this module change.

- Review persistence-related changes manually for indexing, business-key
  semantics, and migration output whenever models move.

- Document any skipped provider, network, or credential-dependent checks in
  the final task summary.

## Related Skills

- src/api/skills/gen-mdf-proxy/SKILL.md

- `src/api/SKILL.md`

- `SKILL.md`

- Create a new backend skill under `src/api/skills/<topic>/SKILL.md` when this
  module grows a repeatable multi-step debugging workflow.
